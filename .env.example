# HubSpot Private App token (required)
# Create one at: HubSpot → Settings → Integrations → Private Apps
HUBSPOT_ACCESS_TOKEN=your-private-app-token-here

# Path to the Gmail OAuth credentials JSON downloaded from Google Cloud Console
# Default: credentials.json in the working directory
GMAIL_CREDENTIALS_FILE=credentials.json
