# HubSpot Private App access token
# Create one at: Settings → Integrations → Private Apps
HUBSPOT_ACCESS_TOKEN=pat-eu1-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Path to Google OAuth2 credentials JSON (downloaded from Google Cloud Console)
# Required scopes: gmail.readonly
GOOGLE_CREDENTIALS=credentials.json

# Path where the OAuth token will be stored after first authentication
GOOGLE_TOKEN=state/gmail_token.json
