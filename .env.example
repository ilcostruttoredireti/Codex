# Gmail OAuth2 — download from Google Cloud Console → APIs & Services → Credentials
GMAIL_CREDENTIALS_FILE=credentials.json
GMAIL_TOKEN_FILE=gmail_token.json

# HubSpot Private App access token (Settings → Integrations → Private Apps)
HUBSPOT_ACCESS_TOKEN=pat-na1-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Your own email addresses (comma-separated) — these senders are skipped
OWN_EMAILS=cristian.mameli.editore@gmail.com,pubblica.latestata@gmail.com

# How many hours back to scan on each run (default: 24)
SYNC_LOOKBACK_HOURS=24
