{
  "name": "gmail-hubspot-sync",
  "version": "1.0.0",
  "description": "Sync Gmail incoming contacts to HubSpot CRM automatically",
  "main": "src/sync.js",
  "scripts": {
    "sync": "node src/sync.js",
    "watch": "node src/watch.js"
  },
  "dependencies": {
    "googleapis": "^140.0.0",
    "@hubspot/api-client": "^11.0.0",
    "dotenv": "^16.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
