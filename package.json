{
  "name": "gmail-hubspot-sync",
  "version": "1.0.0",
  "description": "Sync Gmail inbound contacts to HubSpot automatically",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "sync": "node src/index.js --once",
    "test": "node src/index.js --dry-run"
  },
  "dependencies": {
    "@hubspot/api-client": "^11.0.0",
    "dotenv": "^16.0.0",
    "googleapis": "^144.0.0",
    "node-cron": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
